.\" SPDX-License-Identifier: ISC
.\" SPDX-FileCopyrightText: {{year}} {{authors}}
.Dd $Mdocdate: {{mdocdate}} $
.Dt {{daemon_name | upcase}} 8
.Os
.Sh NAME
.Nm {{daemon_name}}
.Nd example daemon
.Sh SYNOPSIS
.Nm {{daemon_name}}
.Op Fl dnv
.Op Fl f Ar file
.Op Fl s Ar socket
.Sh DESCRIPTION
.Nm
is a template daemon.
It detaches from the terminal, logs to
.Xr syslogd 8 ,
listens on a control socket for
.Xr {{ctl_name}} 8
and runs a periodic task.
.Pp
.Nm
is split into two processes.
The parent keeps its privileges, reads the configuration file and
supervises the engine.
The engine runs the periodic task and serves the control socket; it
receives its configuration and the socket from the parent and never
touches the file system.
When
.Cm user
is set in the configuration file the engine runs as that user, confined
with
.Xr chroot 2
to the
.Cm chroot
directory or the user's home directory.
.Pp
The options are as follows:
.Bl -tag -width Ds
.It Fl d
Do not daemonize.
If this option is specified,
.Nm
will run in the foreground and log to
.Em stderr .
.It Fl f Ar file
Specify an alternative configuration file.
.It Fl n
Configtest mode.
Only check the configuration file for validity.
.It Fl s Ar socket
Use
.Ar socket
instead of the path given in the configuration file to communicate with
.Xr {{ctl_name}} 8 .
.It Fl v
Produce more verbose output.
Once enables debug messages, twice enables trace messages.
.El
.Pp
When
.Cm user
is set in the configuration file,
.Nm
must be started as root.
The control socket is created by the parent, mode 0660 and owned by
root, before the engine drops privileges.
Requests other than
.Cm show status
are accepted only from root and from the user the engine runs as.
.Pp
.Nm
refuses to start when another instance answers on the control socket.
A stale socket left by a crashed instance is removed.
.Pp
If the engine exits unexpectedly the parent logs the event and exits
with status 1.
If the parent goes away the engine exits.
.Pp
On
.Ox
both processes are restricted with
.Xr pledge 2
and
.Xr unveil 2 .
The parent keeps
.Dq stdio rpath cpath proc
with only the configuration file and the control socket visible.
The engine runs with
.Dq stdio unix recvfd
until the parent has handed over the control socket and the
configuration, then
.Dq stdio unix ,
with no file system access at all.
On Linux, when built with the
.Cm landlock
feature, the same file system restrictions are applied with Landlock.
.Pp
The following signals are handled:
.Bl -tag -width SIGTERM
.It Dv SIGHUP
Reload the configuration file.
On error the running configuration is kept.
Changes to
.Cm socket ,
.Cm user
and
.Cm chroot
take effect only after a restart.
.It Dv SIGTERM , Dv SIGINT
Terminate.
.El
.Sh FILES
.Bl -tag -width /var/run/{{daemon_name}}.sock -compact
.It Pa /etc/{{daemon_name}}.conf
Default configuration file.
.It Pa /var/run/{{daemon_name}}.sock
Default control socket.
.El
.Sh EXIT STATUS
.Ex -std
.Sh EXAMPLES
Check the configuration file:
.Pp
.Dl # {{daemon_name}} -n
.Pp
Run in the foreground with debug output on a private socket:
.Pp
.Dl $ {{daemon_name}} -dv -s /tmp/{{daemon_name}}.sock -f {{daemon_name}}.conf
.Sh SEE ALSO
.Xr {{daemon_name}}.conf 5 ,
.Xr {{ctl_name}} 8
.Sh AUTHORS
.An {{author_name}} Aq Mt {{author_email}}
