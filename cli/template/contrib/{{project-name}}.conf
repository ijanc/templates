# {{project-name}} configuration - see {{project-name}}.conf(5)
# Default location: ~/.config/{{project-name}}/config.toml

# Name used in the greeting. Must not be empty.
#name = "world"

# Example numeric setting. Must be at most 10.
#retries = 3
